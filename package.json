{
  "name": "everything-claude-code",
  "version": "1.0.0",
  "description": "ECC plugin for Unity/C++ game development — Claude Code agent harness optimized for Unity and native C++ workflows.",
  "private": true,
  "scripts": {
    "test": "node tests/run-all.js",
    "coverage": "c8 --all --include=\"scripts/**/*.js\" --check-coverage --lines 80 --functions 80 --branches 80 --statements 80 --reporter=text --reporter=lcov node tests/run-all.js"
  },
  "dependencies": {
    "@iarna/toml": "^2.2.5",
    "ajv": "^8.18.0",
    "sql.js": "^1.14.1"
  },
  "devDependencies": {
    "c8": "^10.1.2",
    "markdownlint-cli": "^0.47.0"
  },
  "engines": {
    "node": ">=18"
  }
}
